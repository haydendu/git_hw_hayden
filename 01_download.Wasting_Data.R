{r, eval=FALSE}
install.packages("readr")
library(readr)
data <- read_csv(
  
  
git config --global user.email "haydendu@bu.edu"
git config --global user.name "haydendu"

{r, eval=FALSE}
library(ggplot2)
boxplot(data$`Average WD (%)`~Transect,data=data, main="not sure what to put here",
        xlab="Transect", ylab="Average wasting disease %")